language: python
python:
  - 3.6
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pytest -v --with-coverage --cover-package=routes tests/*
#run coverage tests
after_success:
  - coveralls

