language: python
python:
  - 3.6
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pytest -v tests/*  coveralls --cov-report term-missing
#run coverage tests
after_success:
  - coveralls

